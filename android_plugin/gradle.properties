# 8 GB machine with the editor open: a 2 GB daemon plus a separate Kotlin
# daemon crashed the JVM (malloc failure). One JVM, modest heap, builds fine.
org.gradle.jvmargs=-Xmx1024m
kotlin.compiler.execution.strategy=in-process
android.useAndroidX=true
